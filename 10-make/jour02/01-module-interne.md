# les modules internes de Make

- Flow Control
    - Router xxxx
    - Array Agregator  
        - module précédent j'ai des bundles => rassembler 1 tableau
    - Iterator
        - module précédent j'ai un tableau => boucle pour chaque item du tableau

```js
const a = [1,2,3]
// itérator
for(let element in a)
{
}
```
    - Repeater 

```js
const a = [1,2,3]
for(let i = 0 ; i < 2 ; i++)
{
    a[i]
}
```


- Tools
    - getter / setter (créer ou récupérer des variables)
    - sleep = arrêter le scénario pendant une durée pour mettre un délai pour ne pas soliciter trop rapidement un module après
    - debounce / throttle <https://www.youtube.com/watch?v=JLrYtOgvuPk>

    - aggregator (array / numeric / text)
    - 

- parser => Text / JSON / CSV / Excel  / RSS
- webhooks
- HTTP



- 
- 