# Lecture Ecriture


=> Input Output 
=> IO

=> la capacité pour un utilisateur d'interagir avec une programme

=> le programme lui demande de saisir un mot de passe 
=> si c'est correct le programme lui dit bonjour
=> si c'est incorrect le programme lui dit "identifiants invalides"

```txt
algo io

variable 
prenom , phrase : string
age    : int

debut
    prenom <- Ecrire("Quel est votre prenom ??") Malik
    // Ecrire() est une fonction qui vient de notre pseudo code 
    
    Ecrire("quel est votre age ??") 50
    Lire(age) 
    // Lire() est une fonction native de notre pseudo langage 
    // elle permet de récupérer la valeur Ecrite par l'utilisateur

    // additionner les valeurs saisies par notre utilisateur 
    // concaténation = additionner du texte avec un autre texte ou des int 

    // écrire le texte suivant
    // bonjour prenom , tu as age ans 

    // CONCATENATION
    phrase <- 'bonjour ' & prenom & ' , tu as ' & age & ' ans'

    // ="bonjour "&A1&" tu as "&B1& " ans"

fin 

```